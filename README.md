# POWWOWtest

How to run: 

1. inside docker_file_db folder: 

docker compose up --build  


2. inside docker_file_multiprocess:

docker compose up --build  


3. inside mcc_v1:

docker compose up --build