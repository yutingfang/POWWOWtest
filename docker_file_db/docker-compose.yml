version: "3.9"
services:

    receiver:
        build:
            dockerfile: Dockerfile_rec

    analysis:
        build:
            dockerfile: Dockerfile_ana
        depends_on:
            - receiver
    # volumes:
    #     buffer: